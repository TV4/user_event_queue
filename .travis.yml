os: linux
language: elixir
elixir: 1.10.3
otp_release: 23.0.2
cache:
  directories:
    - _build
    - deps
script:
  - mix format --check-formatted || travis_terminate 1
  - mix test --trace || travis_terminate 1
